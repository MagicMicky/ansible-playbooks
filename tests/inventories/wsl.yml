---
# Test inventory for WSL environment simulation
# Used for testing WSL playbook in Docker with WSL environment variables

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

  vars:
    # Skip homebrew on Ubuntu/WSL
    configure_homebrew: false

    # Test user settings
    # Run as testuser by default, only use become for specific tasks that need root
    ansible_user: testuser

    # WSL-specific environment variables (set via docker compose)
    # WSL_DISTRO_NAME: Ubuntu-22.04
    # WSL_INTEROP: /run/WSL/interop

    # Machine profile for shell configuration
    machine_profile: wsl

    # Test environment dotfiles configuration
    # Clone from public GitHub repo (simulates real deployment)
    dotfiles_dest: "{{ ansible_env.HOME }}/dotfiles-test"
    dotfiles_repo_url: "https://github.com/MagicMicky/dotfiles.git"
    dotfiles_branch: main
